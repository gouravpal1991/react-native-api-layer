// Define your API response data structure here
export interface ApiResponse {
  // Add the properties based on your API response data
  data: any;
  success: boolean;
  message: string;
}

// Define your API request data structure here
export interface ApiRequest {
  // Add the properties based on your API request data
  // For example: params, body, etc.
}
